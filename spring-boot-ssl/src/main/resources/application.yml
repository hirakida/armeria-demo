spring:
  output:
    ansi:
      enabled: always
armeria:
  ports:
    - port: 8080
      protocols: HTTP
    - port: 8443
      protocols: HTTPS
  ssl:
    key-store: keystore.p12
    key-store-password: hirakida
    key-alias: armeria
    key-store-type: PKCS12
