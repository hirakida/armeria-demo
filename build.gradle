plugins {
    id 'org.springframework.boot' version '2.7.0' apply false
    id 'io.spring.dependency-management' version '1.0.11.RELEASE' apply false
    id 'com.google.protobuf' version '0.8.18' apply false
    id 'org.jruyi.thrift' version '0.4.2' apply false
    id 'org.jetbrains.kotlin.jvm' apply false
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    apply plugin: 'io.spring.dependency-management'

    group = 'com.example'
    version = '0.0.1-SNAPSHOT'
    sourceCompatibility = JavaVersion.VERSION_11

    repositories {
        mavenCentral()
    }

    dependencyManagement {
        dependencies {
            dependency 'org.apache.thrift:libthrift:0.16.0'
            dependency 'com.google.protobuf:protoc:3.21.1'
            dependency 'io.grpc:protoc-gen-grpc-java:1.47.0'
            dependency 'org.projectlombok:lombok:1.18.24'

            dependencySet(group: 'com.squareup.retrofit2', version: '2.9.0') {
                entry 'converter-jackson'
                entry 'adapter-rxjava3'
            }
        }
    }

    dependencies {
        implementation platform('com.linecorp.armeria:armeria-bom:1.16.0')
//        implementation platform('io.netty:netty-bom:4.1.76.Final')
        implementation 'org.jetbrains:annotations:23.0.0'
    }

    test {
        useJUnitPlatform()
    }
}
